name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  test:
    name: Test (i18n + E2E)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Verify manifest version matches tag
        run: |
          TAG_VERSION="${{ github.ref_name }}"
          TAG_VERSION="${TAG_VERSION#v}"
          MANIFEST_VERSION=$(node -p "require('./manifest.json').version")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if [ "$TAG_VERSION" != "$MANIFEST_VERSION" ]; then
            echo "❌ Tag $TAG_VERSION does not match manifest version $MANIFEST_VERSION"
            exit 1
          fi
          if [ "$MANIFEST_VERSION" != "$PKG_VERSION" ]; then
            echo "❌ manifest version $MANIFEST_VERSION does not match package.json version $PKG_VERSION"
            exit 1
          fi
          echo "✅ Version match: $MANIFEST_VERSION (manifest + package.json + tag)"

      - name: Run lint & tests
        run: |
          npm run lint
          npm test

  release:
    name: Build & Release
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build extension zip
        run: |
          mkdir -p dist
          zip -r dist/deeppage-${{ github.ref_name }}.zip \
            manifest.json \
            js/ \
            icons/ \
            options.html \
            LICENSE \
            README.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/deeppage-*.zip
          generate_release_notes: true
