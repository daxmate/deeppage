{
  "name": "deeppage",
  "version": "0.0.0",
  "private": true,
  "description": "DeepPage — Chat with DeepSeek while browsing",
  "scripts": {
    "test": "node test/btn-drag-test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
