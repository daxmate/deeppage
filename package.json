{
  "name": "deeppage",
  "version": "0.0.0",
  "private": true,
  "description": "DeepPage — Chat with DeepSeek while browsing",
  "scripts": {
    "test": "playwright test tests/",
    "test:ui": "playwright test --ui tests/",
    "check:i18n": "node scripts/check-i18n.mjs",
    "pretest": "npm run check:i18n"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
